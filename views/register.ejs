<%- include('partials/head', { title }) %>
<%- include('partials/navbar') %>

<main class="mx-auto flex w-full max-w-xl flex-1 flex-col px-4 py-10 sm:px-6 lg:px-8">
  <div class="rounded-2xl bg-white p-8 shadow-sm">
    <h1 class="text-2xl font-semibold text-primary-700">สมัครสมาชิก</h1>
    <p class="mt-2 text-sm text-slate-600">
      กรุณากรอกข้อมูลด้านล่างเพื่อสร้างบัญชีใหม่ บัญชีใหม่จะได้รับสิทธิ์เป็นเจ้าหน้าที่โดยอัตโนมัติและต้องรอผู้ดูแลระบบยืนยันก่อนเข้าสู่ระบบ
      <% if (isAdmin) { %>
        (ผู้ดูแลระบบสามารถกำหนดบทบาทได้)
      <% } %>
    </p>

    <%- include('partials/flash') %>

    <form action="/register" method="post" class="mt-6 space-y-5">
      <div>
        <label for="name" class="block text-sm font-medium text-slate-700">ชื่อ - นามสกุล</label>
        <input
          type="text"
          id="name"
          name="name"
          required
          autocomplete="name"
          class="mt-1 w-full rounded-lg border border-slate-200 px-3 py-2 text-sm shadow-sm focus:border-primary-400 focus:outline-none focus:ring-2 focus:ring-primary-200"
        />
      </div>
      <div>
        <label for="email" class="block text-sm font-medium text-slate-700">อีเมล</label>
        <input
          type="email"
          id="email"
          name="email"
          required
          autocomplete="email"
          class="mt-1 w-full rounded-lg border border-slate-200 px-3 py-2 text-sm shadow-sm focus:border-primary-400 focus:outline-none focus:ring-2 focus:ring-primary-200"
        />
      </div>
      <div>
        <label for="phone" class="block text-sm font-medium text-slate-700">เบอร์โทรศัพท์</label>
        <input
          type="tel"
          id="phone"
          name="phone"
          required
          autocomplete="tel"
          class="mt-1 w-full rounded-lg border border-slate-200 px-3 py-2 text-sm shadow-sm focus:border-primary-400 focus:outline-none focus:ring-2 focus:ring-primary-200"
        />
      </div>
      <div>
        <label for="position" class="block text-sm font-medium text-slate-700">ตำแหน่ง</label>
        <input
          type="text"
          id="position"
          name="position"
          required
          class="mt-1 w-full rounded-lg border border-slate-200 px-3 py-2 text-sm shadow-sm focus:border-primary-400 focus:outline-none focus:ring-2 focus:ring-primary-200"
        />
      </div>
      <div>
        <label for="affiliation" class="block text-sm font-medium text-slate-700">สังกัด / หน่วยงาน</label>
        <input
          type="text"
          id="affiliation"
          name="affiliation"
          required
          class="mt-1 w-full rounded-lg border border-slate-200 px-3 py-2 text-sm shadow-sm focus:border-primary-400 focus:outline-none focus:ring-2 focus:ring-primary-200"
        />
      </div>
      <div>
        <label for="password" class="block text-sm font-medium text-slate-700">รหัสผ่าน</label>
        <input
          type="password"
          id="password"
          name="password"
          required
          minlength="6"
          class="mt-1 w-full rounded-lg border border-slate-200 px-3 py-2 text-sm shadow-sm focus:border-primary-400 focus:outline-none focus:ring-2 focus:ring-primary-200"
        />
        <p class="mt-1 text-xs text-slate-500">ควรตั้งรหัสผ่านไม่น้อยกว่า 6 ตัวอักษร</p>
      </div>
      <% if (isAdmin) { %>
        <div>
          <label for="role" class="block text-sm font-medium text-slate-700">บทบาท</label>
          <select
            id="role"
            name="role"
            class="mt-1 w-full rounded-lg border border-slate-200 px-3 py-2 text-sm shadow-sm focus:border-primary-400 focus:outline-none focus:ring-2 focus:ring-primary-200"
          >
            <option value="staff" selected>เจ้าหน้าที่</option>
            <option value="admin">ผู้ดูแลระบบ</option>
          </select>
        </div>
      <% } %>
      <button
        type="submit"
        class="w-full rounded-lg bg-primary-600 px-4 py-2.5 text-sm font-semibold text-white transition hover:bg-primary-700"
      >
        สมัครสมาชิก
      </button>
    </form>
    <p class="mt-6 text-center text-xs text-slate-500">
      มีบัญชีอยู่แล้ว? <a href="/login" class="font-medium text-primary-600 hover:text-primary-700">เข้าสู่ระบบ</a>
    </p>
  </div>
</main>

<%- include('partials/footer') %>
